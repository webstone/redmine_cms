<% if @page = Page.includes([:attachments, :parts, :pages_parts]).includes(:parts => :attachments).find_by_name(RedmineCms.settings[:landing_page]) %>
   <%= render :file => 'plugins/redmine_cms/app/views/pages/show.html.erb' %>
<% else %>
  <%= render :file => 'app/views/welcome/index.html.erb' %>
<% end %>