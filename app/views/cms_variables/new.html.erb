<h2><%=l(:label_cms_variable_new)%></h2>

<%= labelled_form_for(@variable, :html => {:class => 'tabular'}) do |f|%>
  <%= render :partial => 'form', :locals => { :f => f} %>
  <p>
    <%= f.submit l(:button_create) %>
  </p>
<% end %>